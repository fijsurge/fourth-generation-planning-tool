<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:label="4th Gen Planner"
        android:icon="@mipmap/ic_launcher"
        android:theme="@android:style/Theme.DeviceDefault">

        <!-- Tile service — shows this week's goals on the watch face -->
        <service
            android:name=".WearTileService"
            android:exported="true"
            android:permission="com.google.android.wearable.permission.BIND_TILE_PROVIDER">
            <intent-filter>
                <action android:name="androidx.wear.tiles.action.BIND_TILE_PROVIDER" />
            </intent-filter>
            <meta-data
                android:name="androidx.wear.tiles.PREVIEW"
                android:resource="@android:color/transparent" />
        </service>

        <!-- Listener service — receives goal data pushed from the phone app -->
        <service
            android:name=".WearListenerService"
            android:exported="true"
            android:directBootAware="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                <data
                    android:scheme="wear"
                    android:host="*"
                    android:path="/goals/current" />
            </intent-filter>
        </service>

        <!-- Toggle activity — launched by tile's toggle button -->
        <activity
            android:name=".ToggleGroupActivity"
            android:exported="true"
            android:taskAffinity=""
            android:excludeFromRecents="true" />

        <!-- CycleStatusActivity — launched by tapping a goal chip in the tile -->
        <activity
            android:name=".CycleStatusActivity"
            android:exported="true"
            android:taskAffinity=""
            android:excludeFromRecents="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <!-- GoalsActivity — full scrollable goals list; also the app launcher entry point -->
        <activity
            android:name=".GoalsActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
